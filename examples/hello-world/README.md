# Hello World sample capsule

This is the reviewable input used by `capsule demo`. Run the demo without setup:

```sh
capsule demo
```

It writes a fresh temporary folder, prints the capability review, and names the
generated `capsule.json`. The sample starts only when you explicitly run the
printed `capsule run --config …` command with a rootless engine.
